{% extends 'base.html.twig' %}

{% block title %}Listes des artistes{% endblock %}

{% block body %}
<div class="container">
    <h1 class="pt-2 text-center" style="color:white">Listes des artistes</h1>
    <hr>
    <div class="row pt-2">
    {% for leArtiste in lesArtistes %}
        {% set nbAlbums = leArtiste.albums|length %}
        
            <div class="col-sm-12 col-md-12 col-lg-4 col-xl">
                <div class="card mb-3">
                <h3 class="card-header" style="color:red"> <a href="{{ path('ficheArtiste', {'id': leArtiste.id })}}">{{ leArtiste.nom }}</a> </h3>

            <div class="card-body">

                <h5 class="card-title"> 
                <a href="" style='color:red;font-weight: bold;'class="badge badge-success">{{nbAlbums}} {% if nbAlbums>1 %} albums {% else %} album {% endif %} </a>
                </h5>
            </div>
            <img class"mx-auto d-block" style="border-radius: 60%;height:auto; width:200px; margin:auto; display:block;" src="{{leArtiste.image}}" alt="Card image">
            <div class="card-body">
                <p class="card-text">{{leArtiste.description|slice(0,170)|raw}}...</p>
            </div>
            <ul class="list-group list-group-flush">
                <li class="list-group-item">
                {% if leArtiste.type == 0 %} Artiste solo {% else %} Groupe {% endif %}
                </li>
                <li class="list-group-item"><a href="{{leArtiste.site}}">Site web</a></li>
            </ul>
            </div>
        </div>
    {% endfor %}
    </div>  
</div>
{% endblock %}
